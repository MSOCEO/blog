[build]
  command = "mkdir -p out && cp -r public/* out/"
  publish = "out"

# 全站 301 指向 Cloudflare Pages
[[redirects]]
  from = "/*"
  to = "https://msoceo.pages.dev/:splat"
  status = 301
  force = true
